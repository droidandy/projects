import styled from 'styled-components';
import { Box } from 'grid-styled';
import { error1 } from 'context/palette';

const SimpleError = styled(Box)`
  color: ${error1};
  font-size: 12px;
`;

export default SimpleError;
