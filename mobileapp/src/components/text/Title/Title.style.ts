import { StyleSheet } from 'react-native';
import { theme } from '../../../helpers/theme';

export const titleStyle = StyleSheet.create({
  text: { color: theme.green, fontSize: 20, fontFamily: theme.fonts.normal },
});
