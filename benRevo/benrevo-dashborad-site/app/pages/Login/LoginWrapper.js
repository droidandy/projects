import styled from 'styled-components';

export const LoginWrapper = styled.div`
  flex: 1;
  .container {
    margin: 0 auto;

    .message {
      max-width: 700px;
      margin: 30px auto;
    }
  }
  .auth0-lock-name {
    line-height: 25px;
  }
`;
