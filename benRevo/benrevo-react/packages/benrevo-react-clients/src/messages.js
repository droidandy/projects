/*
 * ClientPage Messages
 *
 * This contains all the text for the ClientPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.ClientPage.header',
    defaultMessage: 'This is ClientPage container.  It doesn\'t require authorization (yet).',
  },
});
