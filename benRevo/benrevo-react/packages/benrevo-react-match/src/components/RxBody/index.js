import { connect } from 'react-redux';
import RxBody from './RxBody';

function mapStateToProps() {
  return {};
}

function mapDispatchToProps() {
  return {};
}
export default connect(mapStateToProps, mapDispatchToProps)(RxBody);
