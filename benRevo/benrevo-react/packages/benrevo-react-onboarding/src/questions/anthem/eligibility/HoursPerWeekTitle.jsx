import React from 'react';
import PropTypes from 'prop-types';

export default function HoursPerWeekTitle() {
  return (
    <div>
      Hours per week for full time employees <span className="rfpPageFormSetHeadingBlue">(minimum 30 hours per week)</span>
    </div>
  );
}
