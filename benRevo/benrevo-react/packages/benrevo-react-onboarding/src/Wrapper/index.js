import { connect } from 'react-redux';
import WrapperOnboarding from './WrapperOnboarding';

function mapStateToProps() {
  return {};
}

function mapDispatchToProps() {
  return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(WrapperOnboarding);
