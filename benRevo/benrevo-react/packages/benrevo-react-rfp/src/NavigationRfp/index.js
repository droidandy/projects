import { connect } from 'react-redux';
import NavigationRfp from './NavigationRfp';

function mapDispatchToProps() {
  return {

  };
}

export default connect(null, mapDispatchToProps)(NavigationRfp);
