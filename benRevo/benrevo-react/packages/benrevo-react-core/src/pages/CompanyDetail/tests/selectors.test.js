// import { fromJS } from 'immutable';
// import { makeSelectCompanyDetailPageDomain } from '../selectors';

// const selector = makeSelectCompanyDetailPageDomain();

describe('makeSelectCompanyDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
