import { connect } from 'react-redux';
import PlansAnthem from './PlansAnthem';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps() {
  return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(PlansAnthem);
