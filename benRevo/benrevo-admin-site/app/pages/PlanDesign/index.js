import { connect } from 'react-redux';
import PlanDesign from './PlanDesign';

function mapStateToProps() {
  return {
  };
}

function mapDispatchToProps(/* dispatch */) {
  return {
  };
}

export default connect(mapStateToProps, mapDispatchToProps)(PlanDesign);
