{% assign lang = "ru" %}
{%if attributes.lang%}{% assign lang = attributes.lang %}{%endif%}

{% comment %} -------------------- translations_begin --------------------{% endcomment %}
{% capture email_title %}Восстановление пароля{% endcapture %}
{%if lang == "en"%}
  {% capture email_title %}Password reset{% endcapture %}
{%endif%}


{% capture email_body %}Нажмите на ссылку ниже, чтобы указать новый пароль для вашего аккаунта CREAM.LY.{% endcapture %}
{%if lang == "en"%}
  {% capture email_body %}Click on the link below to specify a new password for your CREAM.LY account.{% endcapture %}
{%endif%}

{% capture translation_signIn %}Войти в аккаунт{% endcapture %}
{%if lang == "en"%}
  {% capture translation_signIn %}Sign in{% endcapture %}
{%endif%}

{% capture translation_questions %}Если у вас есть вопросы, просто ответьте на этой имэйл или напишите на <a href="mailto:{{ shop.email }}">{{ shop.email }}</a>{% endcapture %}
{%if lang == "en"%}
  {% capture translation_questions %}If you have any questions, reply to this email or contact us at <a href="mailto:{{ shop.email }}">{{ shop.email }}</a>{% endcapture %}
{%endif%}

{% capture translation_visit %} Перейти на сайт {% endcapture %}
{%if lang == "en"%}
  {% capture translation_visit %} Visit our store {% endcapture %}
{%endif%}

{% capture translation_or %} или {% endcapture %}
{%if lang == "en"%}
  {% capture translation_or %} or {% endcapture %}
{%endif%}

{% capture translation_new %} Ввести новый пароль {% endcapture %}
{%if lang == "en"%}
  {% capture translation_new %} Enter new password {% endcapture %}
{%endif%}

<!DOCTYPE html>
<html lang="ru">
  <head>
  <title>{{ email_title }}</title>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=device-width">
  <link rel="stylesheet" type="text/css" href="/assets/notifications/styles.css">
  
  <style>
    .button__cell { background: {{ shop.email_accent_color }}; }
    a, a:hover, a:active, a:visited { color: {{ shop.email_accent_color }}; }
  </style>
</head>

  <body>
    <table class="body">
      <tr>
        <td>
          <table class="header row">
  <tr>
    <td class="header__cell">
      <center>

        <table class="container">
          <tr>
            <td>

              <table class="row">
                <tr>
                  <td class="shop-name__cell">
                    {%- if shop.email_logo_url %}
                      <img src="{{shop.email_logo_url}}" alt="{{ shop.name }}" width="{{ shop.email_logo_width }}">
                    {%- else %}
                      <h1 class="shop-name__text">
                        <a href="{{shop.url}}">{{ shop.name }}</a>
                      </h1>
                    {%- endif %}
                  </td>

                </tr>
              </table>

            </td>
          </tr>
        </table>

      </center>
    </td>
  </tr>
</table>

          <table class="row content">
  <tr>
    <td class="content__cell">
      <center>
        <table class="container">
          <tr>
            <td>
              
            <h2>{{ email_title }}</h2>
            <p>{{ email_body }}</p>
            <table class="row actions">
  <tr>
    <td class="empty-line"> </td>
  </tr>
  <tr>
    <td class="actions__cell">
      <table class="button main-action-cell">
        <tr>
          <td class="button__cell"><a href="{{ customer.reset_password_url }}" class="button__text">{{translation_new}}</a></td>
        </tr>
      </table>
{% comment %}
      {% if shop.url %}
    <table class="link secondary-action-cell">
      <tr>
        <td class="link__cell">{{translation_or}} <a href="{{ shop.url }}">{{translation_visit}}</a></td>
      </tr>
    </table>
{% endif %}
{% endcomment %}
    </td>
  </tr>
</table>


            </td>
          </tr>
        </table>
      </center>
    </td>
  </tr>
</table>
          <table class="row footer">
  <tr>
    <td class="footer__cell">
      <center>
        <table class="container">
          <tr>
            <td>
              
             <p class="disclaimer__subtext">{{translation_questions}}</p>
            </td>
          </tr>
        </table>
      </center>
    </td>
  </tr>
</table>

<img src="{{ 'notifications/spacer.png' | shopify_asset_url }}" class="spacer" height="1" />

        </td>
      </tr>
    </table>
  </body>
</html>
