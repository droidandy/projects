/* import * as checkoutManager from "@Core/checkoutManager";
import { getAllUrlParams } from "@Core/url";

const token = getAllUrlParams().token;
if (token) checkoutManager.redirectIfPaid(token, 10);
 */
