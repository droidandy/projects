import { IocContainer, IocModule } from '@invest.wl/core';

export class SFirebaseModule extends IocModule {
  public configure(ioc: IocContainer): void {
    // ioc.bind<I>(Tid).to().inSingletonScope();
  }
}
