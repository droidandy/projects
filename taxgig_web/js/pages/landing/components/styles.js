import styled from "styled-components";

import { LandingWrapperPadding } from "../../../components/styles";

export const MainContent = styled.div`
	// position: absolute;
	top: 0;
	width: 100%;
	// padding-top: calc(54px + 2 * (${LandingWrapperPadding}));
`;
