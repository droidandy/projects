import React from 'react';
import { styled } from '@material-ui/core/styles';

export const ActionsLayer = styled('div')({
  position: 'absolute',
  top: '0.75rem',
  right: '0.75rem',
  zIndex: 2,
});
