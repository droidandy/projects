import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(
  () => ({
    tabsContainer: {
      overflow: 'hidden',
      maxWidth: '100%',
    },
  }),
  { name: 'VehicleAccordion' },
);
