export type LeadSourceInfo = {
  utmSource: string;
  utmContent: string;
  utmMedium?: string;
  utmCampaign?: string;
  utmTerm?: string;
  timestamp: string;
  userId?: string;
  clientId?: string;
  utmUserAgent?: string;
};
