import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(({}) => {
  return {
    icon: {
      fill: 'none',
      marginRight: '0.625rem',
    },
    singleItem: {
      display: 'flex',
    },
  };
});

export { useStyles };
